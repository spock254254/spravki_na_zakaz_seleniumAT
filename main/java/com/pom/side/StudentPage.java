package com.pom.side;

import com.pom.BasePage;
import org.openqa.selenium.WebDriver;

public class StudentPage extends BasePage {
    public StudentPage(WebDriver driver) {
        super(driver);
    }
}
