package com.pom.side;

import com.pom.BasePage;
import org.openqa.selenium.WebDriver;

public class ChildrenPage extends BasePage {
    public ChildrenPage(WebDriver driver) {
        super(driver);
    }
}
